node_modules/
build/
dist/
coverage/
.firebase/
.DS_Store
*.log

# Environment files (contain secrets — never commit)
.env
.env.*
!.env.example

# Firebase service-account keys (never commit)
sa-keys/
